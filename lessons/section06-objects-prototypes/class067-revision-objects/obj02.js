// Utilizando a notação de chaves quando não sabemos qual nome a chave vai receber
const pessoa = {
    nome: 'Breno', // nome é a chave que tem o valor 'Breno'
    sobrenome: 'Rangel'
}

const chave = 'nome'
console.log(pessoa[chave])