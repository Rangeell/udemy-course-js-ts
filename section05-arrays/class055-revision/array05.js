// Resvisando alguns método/atributos básicos
const nomes = ['Breno', 'Maria', 'Joana']
console.log(nomes.length)
const removido = nomes.pop() // Retornar o elemento removido
console.log(nomes, removido)